name: virtual_workshop
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.18.2 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  lottie: ^1.4.3
  url_launcher: ^6.1.6
  http: ^0.13.5
  shimmer: ^2.0.0
  location: ^4.4.0
  carousel_slider: ^4.1.1
  fluttertoast: ^8.1.3
  shared_preferences: ^2.0.17
  webview_flutter: ^4.0.6
  intl: ^0.18.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/
    - assets/no_data.json
